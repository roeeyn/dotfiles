[user]
	name = Rodrigo Medina
[github]
	user = roeeyn
# SSH commit signing everywhere; each profile's .gitconfig.local supplies
# the signingkey registered on the matching GitHub account (personal key
# for the personal account, EMU key for the work account).
[gpg]
	format = ssh
[commit]
	gpgsign = true
[push]
	default = simple
[credential]
	helper = osxkeychain
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
[pull]
  rebase = false
[init]
	defaultBranch = main
[core]
    pager = DELTA_FEATURES=+side-by-side delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = false # use n and N to jump between diff sections
    hyperlinks = false
[merge]
    conflictStyle = zdiff3
[fetch]
	prune = true
# Profile-provided identity and signing (stowed by profiles/personal or the
# private work overlay). Last include wins, so profile values override.
[include]
	path = ~/.gitconfig.local
