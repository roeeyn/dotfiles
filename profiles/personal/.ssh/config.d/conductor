# Conductor writes and owns ~/.ssh/conductor_config; that file is machine-local
# and never tracked here. Sorting before `personal` keeps its Host blocks ahead
# of that file's catch-all `Host *`, matching where the include used to sit.
# A missing target is a silent no-op, so this is safe before Conductor runs.
Include ~/.ssh/conductor_config
